package org.locer.`in`.recyclerView

data class CartItems(val itemName: String,
                     val itemImage: String,
                     val itemCount: Int,
                     val itemPrice: Int)
